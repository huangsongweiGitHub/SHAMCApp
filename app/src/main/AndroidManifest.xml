<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nantian.shamc.approval">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/NoTitleTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainFrameActivity" >
        </activity>
        <activity android:name=".MainWaitingActivity" >
        </activity>
        <activity android:name=".MainAlreadyActivity" >
        </activity>

        <!-- 有关审批的各个类开始 -->
        <activity android:name=".MainApprovalProjectActivity" >
        </activity>
        <!-- 有关审批的各个类结束 -->

    </application>

</manifest>